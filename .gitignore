# Node
node_modules/
dist/
build/
.env
.env.local
.env.*
# ...but do track example env files (no secrets)
!.env.example
!**/.env.example

# Secrets & credentials
serviceAccount.json
service-account.json
service_account.json
*-serviceaccount.json
*serviceAccount*.json
credentials.json
firebase-adminsdk*.json
backend/src/config/serviceAccount.json
*.pem
*.p12
*.key

# Internal notes / planning docs / AI instructions
.claude/

# Python
__pycache__/
*.pyc
.pytest_cache/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test videos & debug output
test-vids/
test_vids/
test vids/
worker/debug_files/
worker/debug_hits_output/
worker/debug_hits.py

# Datasets (huge; videos re-downloadable via ShuttleSet/download_videos.py)
ShuttleSet/

# Model weights & training artifacts (live in Modal volume / R2, not git)
worker/weights/
worker/train/features_v3/

# Vendored-reference scratch (BST paper sources, validation script)
.bst-ref/
